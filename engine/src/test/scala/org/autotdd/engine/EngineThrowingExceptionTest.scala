package org.autotdd.engine

import org.junit.runner.RunWith
import org.scalatest.junit.JUnitRunner

@RunWith(classOf[JUnitRunner])
class EngineThrowingExceptionTest extends EngineStringStringTests {

  "An Engine" should "Allow an excepted result to be an exception" in {
//    builderWithUseCase.scenario("e", "Throws Exception").
//      expectException((s) => new RuntimeException("with some message")).
//      build

  }

}